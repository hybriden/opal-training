services:
  - type: web
    name: opal-text-transformer
    runtime: node
    rootDir: text-transformer-opal
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
